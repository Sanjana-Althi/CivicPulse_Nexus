spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/civicpulse
    username: postgres
    password: Sanjana@12

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect